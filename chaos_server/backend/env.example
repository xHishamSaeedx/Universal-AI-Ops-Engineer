# Chaos Server Environment Configuration

# Application
APP_NAME=Chaos Server API
DEBUG=true
VERSION=0.1.0

# Server
HOST=0.0.0.0
PORT=8080

# API
API_V1_PREFIX=/api/v1

# CORS (comma-separated origins, or * for all)
CORS_ALLOW_ORIGINS=*

# Target server (victim) base URL
# For local development: http://localhost:8000
# For docker-compose: http://target_server_api:8000
TARGET_API_BASE_URL=http://localhost:8000

# Target server database URL (for direct DB access for migration chaos)
# For local development: postgresql://postgres:password@localhost:5432/target_server
# For docker-compose: postgresql://postgres:password@target_server_db:5432/target_server
TARGET_DATABASE_URL=postgresql://postgres:password@localhost:5432/target_server

# Target server file paths (for env var chaos)
# Paths are relative to workspace root or absolute paths
TARGET_ENV_FILE=target_server/backend/.env
TARGET_COMPOSE_FILE=target_server/docker-compose.yml
